🧩 Problem: Reusing Role-Based Access Logic in a Dashboard App
You're building an admin dashboard for a manufacturing plant using React. The dashboard has different user roles:

Admin
Supervisor
Operator

Certain components (like SettingsPage, ReportsPage, or MachineControls) should only be visible to users with specific roles.

You need to restrict access to pages or components based on the logged-in user's role.