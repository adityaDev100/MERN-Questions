1. What is TypeScript? How is it different from JavaScript?

TypeScript is a superset of JavaScript that adds static types to the language. It's designed to help catch errors during development (compile-time) rather than runtime, which makes development smoother and more predictable.

